---
layout : post
title : "심화 6주차"
---

# 13장 텍스트 유사성 측정

## 13.1 간단한 텍스트 비교

정확한 단어 비교 불가능 이유

1. 일관성 없는 대문자

2. 일관성 없는 구두점

![image](https://github.com/user-attachments/assets/8e7eb02d-dfac-45e6-ab40-21532c8808f8)

자카드 유사도(=자카드 지수): 유사성 지표

1. 두 텍스트가 주어졌을 때 각 텍스트에서 단어 리스트를 추출합니다.

2. 텍스트 간 공유되는 고유 단어 수를 셉니다.

3. 공유 단어 수를 두 텍스트의 총 고유 단어 수로 나눕니다.

![image](https://github.com/user-attachments/assets/eca94804-bca5-40d9-bf58-8e49965aaa9f)

### 13.1.1 자카드 유사도 탐색

자카드 유사도 텍스트 유사성을 합리적으로 측정하는 척도인 이유

1. 텍스트 간 겹치는 단어와 겹치지 않는 단어를 함께 고려합니다.

2. 항상 0과 1 사이로 표현되며 해석이 쉽습니다. 0은 공유 단어가 없음을, 0.5는 절반의 단어가 공유됨을, 1은 모든 단어가 공유됨을 의미합니다.

3. 쉽게 구현할 수 있습니다.

두 set 사이의 모든 단어를 비교하고 대조하는 방식을 효율적 x, 많은 계산 비용 발생

-> 합집합 계산 부분 제거

1. len(word_set_a)와 len(word_set_b)를 더하면 공유 단어 수가 2번 계산된 총 고유 단어 수를 얻을 수 있습니다.

2. 1단계 계산 결과에서 len(word_set_a & word_set_b)를 빼면 중복 계산 된 공유 단어 수를 절반으로 제거할 수 있습니다. 따라서 최종적으로 그 결과는 len(word_set_a | word_set_b)와 같습니다.

문장 수준 수백 개에서는 효율적 but 수천 개 이상에서는 x

넘파이 사용하여 이 연산의 계산 속도 높이기 가능

### 13.1.2 단어를 숫자 값으로 바꾸기

단어를 숫자로 바꾸기: 모든 텍스트의 모든 단어를 반복적으로 접근하며 각 고유 단어에 i 값을 할당해주면 됨.

어휘: 단어와 숫자 값 간의 매핑 정보를 파이썬의 딕셔너리로 저장했을 떄의 딕셔너리

벡터: 1차원 숫자 배열

텍스트의 벡터화: 텍스트를 벡터로 변환하는 과정

가장 간단한 방법은 '이진 요소로 구성된 벡터를 만드는 것'

벡터의 각 인덱스는 어휘 내 한 단어에 연결

일부 어휘 단어가 누락되더라도 벡터 크기 = 어휘 크기

i 인덱스의 단어가 텍스트에서 누락된 경우 벡터의 i번쨰 요소는 0으로 설정, 그렇지 않다면 1로 설정

![image](https://github.com/user-attachments/assets/370c097c-678a-4195-b101-a142bd2be362)

테이블의 행은 텍스트에, 열은 어휘에 매핑

![image](https://github.com/user-attachments/assets/29d6f3e4-a78b-4632-89cb-c75981185293)

이진 벡터 표현은 공유 단어를 숫자로 추출할 수 있게 해줌

내적: 벡터의 요소별 곱을 모두 더하는 것

-> 공유된 단어 수를 구할 수 있고, 이는 당연히 교집합 크기와 일치

타니모토 유사도: 벡터화된 텍스트로 자카드 유사도를 구현한 것

## 13.2 단어 수를 사용하여 텍스트 벡터화하기

이진 벡터는 텍스트에 특정 단어의 포함 유무만 파악할 수 있지만, 각 단어 수까지는 파악 x

용어 빈도 벡터(= TF 벡터): 단어 수 벡터

![image](https://github.com/user-attachments/assets/4bb5fa1a-ab7a-4cdf-af61-26228da32ed9)

TF 벡터는 텍스트 간 단어 수 차이에 민감하지만, 길이가 다른 텍스트를 비교할 떄는 불리

### 13.2.1 정규화로 TF 벡터 유사도 개선하기

검색 엔진 예시: 쿼리를 데이터베이스에 저장된 문서 제목과 비교.

먼저 쿼리는 TF 벡터화되고, 이미 벡터화되어 저장된 모든 문서 제목과 비교

타니모토 유사도가 0이 아닌 제목이 반환되고 유사도 점수에 따라 순위가 매겨짐.

-> 이 경우 텍스트 크기로 인한 순위 차가 날 수 있음

-> 텍스트 크기가 순위 결과에 미치는 영향 억제 필요: title_a_vector을 나누어 query_vector와 크기가 같아지게 함

수학적 관점: 모든 벡터 = 기하학적 객체

![image](https://github.com/user-attachments/assets/f8d19a5d-2050-4303-9a2e-a8cb85802745)

크기(= 유클리드 노름 = 노름: 벡터를 선분으로 시각화하였을 때의 기하학적인 길이

![image](https://github.com/user-attachments/assets/e8581606-72f0-432e-9cd4-5306f5583fb8)

![image](https://github.com/user-attachments/assets/f35bb798-7311-4545-8ff0-99b551df3057)

단위 벡터: 크기가 1인 벡터

-> 장점: 벡터의 크기가 유사도에 영향을 미치지 않고, 방향에 따라서만 결정

정규화: 벡터를 자기 자신의 크기로 나누는 것

![image](https://github.com/user-attachments/assets/ab68f87f-2d71-4623-9747-9505d445e9ac)

두 단위 벡터를 내적하면 두 벡터 사이의 각도와 공간 거리로 쉽게 변환이 가능한 매우 특별한 값을 얻을 수 있음

### 13.2.2 단위 벡터 내적으로 관련성 지표 간 변환하기

![image](https://github.com/user-attachments/assets/47760dd4-2270-4fd1-8a1c-15f062e2e7ce)

타니모토 유사도 / 코사인 유사도 / 유클리드 거리 셋 간의 변환 매우 쉬움

정규화된 벡터 간 타니모토 유사도는 다르 유사도 또는 거리 지표로 변환 가능

1. 타니모토 유사도를 유클리드 거리로 변환하면 텍스트 데이터에 대한 K-평균 클러스터링을 수행할 수 있습니다.

2. 타니모토 유사도를 코사인 유사도로 변환하면 계산을 간소화할 수 있습니다. 모든 계산이 간단한 내적 연산으로 축소됩니다.

![image](https://github.com/user-attachments/assets/e0fb3b6a-ebaa-4284-a406-577a631f1b15)

정규화의 이점

1. 텍스트 길이에 따른 차별성 제거: 비슷한 내용이 담긴 길고 짧은 텍스트 비교 가능

2. 보다 효율적인 타니모토 유사도의 계산: 단일 내적 연산만으로 가능

3. 모든 벡터 쌍들에 대한 보다 효율적인 유사도 계산: 전체 유사도

## 13.3 효율적인 유사도 계산을 위한 행렬 곱셈

각 테스트 쌍을 개별적으로 비교하는 대신 모든 쌍의 유사도를 테이블로 시각화

행과 열은 개별 텍스트와 각 텍스트에 포함된 단어를 구분하고, 각 요소는 타니모토 유사도를 표현

![image](https://github.com/user-attachments/assets/e3db5466-036f-4679-9121-8edc13eb7b75)

![image](https://github.com/user-attachments/assets/94edb7e6-c6ca-4192-bd27-f3deb330fdae)

### 13.3.1 기본 행렬 연산

행렬: 1차원 벡터를 2차원으로 확장한 것(숫자를 테이블 형태로 표현)

#### 넘파이 행렬 산술 연산

넘파이 배열에서 사칙연산 모두 사용 가능

#### 넘파이 행렬의 행과 열 연산

행, 열 출력 가능

내적 계산 가능

![image](https://github.com/user-attachments/assets/8982256f-62d2-4b71-9cff-3070d7d8d270)

#### 넘파이 행렬의 곱

@를 이용하여 행렬 간 행렬의 곱(행렬 곱셈) 계산 가능

![image](https://github.com/user-attachments/assets/572ccf6b-0299-47f1-a537-957766a60cf6)

![image](https://github.com/user-attachments/assets/053aa3b6-e594-4525-aba4-b1ba7adaf317)

넘파이 사용 시 중첩 반복문 업이 행렬 곱셉 수행 가능

### 13.3.2 전체 행렬에 대한 유사도 계산하기

![image](https://github.com/user-attachments/assets/62272193-bef9-4054-a474-8c63d675412c)

![image](https://github.com/user-attachments/assets/f80668fa-ebff-45b3-9803-3be2ec755412)

## 13.4 행렬 곱셈의 계산 한계

행렬 곱셈 계산 속도는 행렬 크기에 따라 결정

![image](https://github.com/user-attachments/assets/5d58aa3c-60ac-4876-8627-7dede19ea9fc)

책 권수 30권(0.005초) -> 1000권(0.27초) -> 30만권(약 7시간)

실행 시간 줄여야함 -> 행렬 크기 줄이기

1. 단 한 번만 언급되거나 다른 소설에서 거의 언급조차 되지 않은 단어 제거

2. 모든 소설에 공통으로 등장하는 일반적인 단어는 텍스트들을 구별 짓는 신호를 제공 안하므로 제거

# 14장 행렬 데이터의 차원 감소

차원 축소: 데이터 정보 내용을 유지한 채 데이터를 축소하는 기술

이점

1. 압축된 데이터는 전송과 저장이 더 쉽습니다.

2. 적은 데이터에 대한 알고리즘 수행 시간은 단축됩니다.

3. 데이터 그룹/클러스터를 파악하는 등 복잡한 작업에서 불필요한 노이즈 정보를 제거하여 복잡함을 간소화할 수 있습니다.

## 14.1 2D 데이터를 단일 차원으로 그룹화하기

![image](https://github.com/user-attachments/assets/13cdc096-4a84-448b-b1e2-017670de6f2a)

맷플롯립은 2D 축을 자동으로 조작하여 그래프를 미적으로 보기 좋게 만듬

데이터 단순화를 위해 그래프를 회전하면 축 배율도 바뀌어 데이터 비교가 어려움

-> 일관된 시각화를 위해 축의 척도 균등화

![image](https://github.com/user-attachments/assets/7a0f8191-c53c-4d5a-9512-1084240de6a1)

동일한 세 부분으로 나누어 크기별 그룹화

![image](https://github.com/user-attachments/assets/512b89d4-e3a0-4dca-96db-e434ba809751)

데이터를 x축 방향으로 회전하면, 세로축에 의존하지 않고 데이터를 그룹화할 수 있어 y축 값 제거 가능

![image](https://github.com/user-attachments/assets/38a6cc11-efa1-43a7-94ad-8684662cf82a)

### 14.1.1 회전으로 차원 줄이기

데이터 뒤집기

1. 좌표(0, 0)의 원점을 중심으로 모든 데이터를 이동(그래프를 x축 방향으로 더 쉽게 회전 가능)

2. 데이터에서 x축까지 총 거리가 최소화될 때까지 데이터를 회전

![image](https://github.com/user-attachments/assets/922a9d7c-4b88-48c3-b881-2920f272b47d)

회전 행렬

![image](https://github.com/user-attachments/assets/94d577fa-a62c-4d34-b77d-64cb766ec534)

페널티와 분산의 연관성

1. 페널티 점수는 0에서 y_values 배열 내 모든 y까지 거리를 제곱한 평균과 같습니다.

2. y_values.mean()은 0입니다.

3. 따라서 페널티 제곱은 평균에서 y까지 거리를 제곱한 평균과 같습니다.

4. 평균에서 거리를 제곱한 평균은 분산과 같습니다.

5. 즉, 페널티 점수는 y_values.var()와 같습니다.

총 분산의 보존

1. x축 분산과 y축 분산을 단일 백분율 점수로 결합할 수 있습니다. 즉, x_values.var() / total_variance는 1 - y_values.var() / total_variance와 동일합니다.

2. 2. 데이터를 x축 방향으로 회전하면 x축 분산이 증가하고 y축 분산은 그에 상응하는 수준으로 감소합니다. 수직 분산을 p% 감소시키면 수평 분산이 p% 증가합니다.
  

## 14.2 PDA와 사이킷런으로 차원 감소시키기



## 14.3 4D 데이터를 2차원으로 그룹화하기



### 14.3.1 PCA의 제한 사항



## 14.4 회전 없이 주성분 계산하기



### 14.1 거듭제곱 반복으로 고유 벡터 추출하기



## 14.5 SVD 및 사이킷런으로 효율적인 차원 축소하기



# 15장 대용량 텍스트에 대한 자연어 처리 분석

## 15.1 사이킷런으로 웹 토론 포럼 데이터셋 불러오기



## 15.2 사이킷런으로 문서 벡터화하기



## 15.3 게시글 빈도 및 개수로 단어의 순위 매기기



## 15.4 대규모 문서 데이터셋의 유사성 계산하기



## 15.5 주제별로 텍스트 그룹화하기



### 15.5.1 단일 텍스트 클러스터 탐색하기



